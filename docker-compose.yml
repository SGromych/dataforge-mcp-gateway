services:
  dataforge-mcp:
    build: .
    env_file: .env
    environment:
      - MCP_TRANSPORT=sse
      - HOST=0.0.0.0
      - PORT=8080
    ports:
      - "8080:8080"
    volumes:
      - cache_data:/app/cache

volumes:
  cache_data:
